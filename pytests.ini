[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    registered_user: tests with registered user
    need_review: test to be review by Stepik members
    login_guest